CREATE PROCEDURE USP_AGENDAMENTO
@AG VARCHAR(50)
AS
BEGIN
	
	IF @AG = '' 
	SELECT *
	FROM AGENDAMENTO WHERE DAY(DT_AGENDAMENTO)  = DAY(GETDATE()) 

	SELECT *
	FROM AGENDAMENTO WHERE DT_AGENDAMENTO LIKE '%'+''+@AG+''+'%'
	
END
